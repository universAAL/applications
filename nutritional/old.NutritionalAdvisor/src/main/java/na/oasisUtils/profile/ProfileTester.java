package na.oasisUtils.profile;

//import org.apache.commons.logging.Log;
//import org.apache.commons.logging.LogFactory;

public class ProfileTester {

//	private static Log log = LogFactory.getLog(ProfileTester.class);
	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// download latest profile
//		ProfileConnector.getInstance().downloadProfileFromServer();
		// set common data
//		ProfileConnector.getInstance().setCommonData();
		// set nutrition data
//		ProfileConnector.getInstance().setNutritionData();
//		ProfileConnector.getInstance().setExtraNutritionData();
		
		
//		log.info("The user's id is: "+ProfileConnector.getInstance().getUserID());
//		log.info("The user's pref lang is: "+ProfileConnector.getInstance().getPreferredLanguage());
//		log.info("The user's name is: "+ProfileConnector.getInstance().getName());
//		log.info("The user's age is: "+ProfileConnector.getInstance().getUserAge());
////		log.info("The user's email is: "+ProfileConnector.getInstance().getEmail());
//		log.info("The user's height is: "+ProfileConnector.getInstance().getHeight());
//		log.info("The user's weight is: "+ProfileConnector.getInstance().getWeight());
////		log.info("The user's spoken languages are: "+ProfileConnector.getInstance().getSpokenLanguages());
//		log.info("The user's favourite recipes are: "+ProfileConnector.getInstance().getFavourite_Recipes());
	}

}
